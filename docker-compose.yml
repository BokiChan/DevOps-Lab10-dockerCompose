services:
  nginx:
     image: nginx:latest
     volumes:
       - ./html:/usr/share/nginx/html
     ports:
       - "8089:80"
     restart: unless-stopped
# 403 my as machine is running on 8080 and i want to use it for other project so i change the port to 8089